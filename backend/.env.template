# Default MONGO_URI for local development.
# This is for local dev only. If using docker-compose,
# you can use the credentials below.
MONGO_URI=mongodb://admin_user:AdminPass123!@127.0.0.1:27017/yarnitt?authSource=admin

# JWT Configuration
# IMPORTANT: Change JWT_SECRET in production to a secure random value
JWT_SECRET=your-secret-key-change-in-production
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Instructions to override via environment variables:
# You can override these values by setting the environment variables in your system or Docker.

# For more details, visit README.dev.md for setup instructions.